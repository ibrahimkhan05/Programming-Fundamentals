#include <stdio.h>

int main (void) {

    int name[10];
    int index = 0;
    for (int i = 1; i <=10; i++) {
        printf("Enter a number: \n");
        scanf("%d", &name[index]);
        
    }
    a
    
    for (int j = 1; j<=10; j++) {
        printf("Value of index: %d\n", name[index]);
        index--;
    }
    return 0;
}