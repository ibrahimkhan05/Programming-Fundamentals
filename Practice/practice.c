#include <stdio.h>
#define pi  3.14159
int main(void)
{

    // int integer = 66;
    // char chracter = 'A';
    // float floatvar = 2.56f;
    // double doublevar = 0.34343434343;
    // int x = 0234;
    // int octal, hexadecimal;
    // int firstNumber = 5, secondNumber = 3;
    // double result, result1;
    // int trunction = 1.33; //for understanding trunction
    // float radius, area;

    // //simple output
    // printf("Hello world\n");

    // // differnt data types and format specifier
    // printf("The octal value of %d is %#o and its hexadecimal value is %#x\n", x, x, x);
    // printf("The interger is %d\n", integer);
    // printf("The interger %d in ASCII chart is %c\n", integer, chracter);
    // printf("The Folat type value is %f\n", floatvar);
    // printf("The expotional form of %f is %e\n", floatvar, floatvar);
    // printf("The double value is %f\n", doublevar);
    // printf("The expoitinal form of %f is %e\n", doublevar, doublevar);

    // //number system
    // printf("Enter your octal number which you want to convert to decimal: ");
    // scanf("%o", &octal);
    // printf("Enter your hexadecimal number which you want to convert to decimal: ");
    // scanf("%x", &hexadecimal);
    // printf("Your octal number: %o in decimal is: %d\n", octal, octal);
    // printf("Your hexadecimal number: %x in decimal is: %d\n", hexadecimal, hexadecimal);

    // //understading diifernernt rules
    // result = firstNumber/secondNumber;

    // //conversting int to double in expression
    // result1 = firstNumber/(double)(secondNumber);
    // printf("Result of interger divided by integer is  %f\nresult of integer divided by double is  = %f\n", result, result1);
    
    // //truncation
    // printf("The result we get while storing floting point value in int datatype is %i\n", trunction);
    
    // //radius or define ie values that are same throught program
    // printf("enter radius of the circle to find its area: ");
    // scanf("%f", &radius);
    // area = pi * (radius * radius);
    // printf("Area of the circle is %f\n", area);
    // int aaa = 2, bb = 2;
    // float cc = aa/bb;
    // printf("bb", %cc)
    // return 0;
    int aa = 150;
    // float cc = aa/100.0;
    
     float cc = (float) 150/100;
    printf("%f", cc);
    return 0;
}