#include <stdio.h>
#include<stdbool.h>

int main () {
    int weekend[] = {true, false, false, false, false, false, true};
    int weekend[] = {[0] = true, [6] = true};
    // printf("%d, %d", weekend[1], weekend[5] );
    // printf("%d, %d", weekend[0], weekend[6] );
    return 0;



}